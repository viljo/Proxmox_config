<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Services - Viljo Infrastructure</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    :root {
      --bg-primary: #0f172a;
      --bg-secondary: #1e293b;
      --bg-card: #334155;
      --text-primary: #f8fafc;
      --text-secondary: #cbd5e1;
      --accent: #3b82f6;
      --accent-hover: #2563eb;
      --border: #475569;
      --success: #10b981;
      --warning: #f59e0b;
      --planned: #6366f1;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
      background: var(--bg-primary);
      color: var(--text-primary);
      min-height: 100vh;
      padding: 2rem 1rem;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
    }

    header {
      text-align: center;
      margin-bottom: 3rem;
      padding-bottom: 2rem;
      border-bottom: 2px solid var(--border);
    }

    h1 {
      font-size: 2.5rem;
      margin-bottom: 0.5rem;
      background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .subtitle {
      color: var(--text-secondary);
      font-size: 1.125rem;
    }

    .section {
      margin-bottom: 3rem;
    }

    .section-header {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      margin-bottom: 1.5rem;
      padding-bottom: 0.75rem;
      border-bottom: 1px solid var(--border);
    }

    .section-title {
      font-size: 1.5rem;
      font-weight: 600;
      color: var(--text-primary);
    }

    .section-badge {
      display: inline-flex;
      align-items: center;
      gap: 0.375rem;
      padding: 0.25rem 0.75rem;
      border-radius: 999px;
      font-size: 0.75rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .badge-success {
      background: rgba(16, 185, 129, 0.2);
      color: var(--success);
      border: 1px solid var(--success);
    }

    .badge-warning {
      background: rgba(245, 158, 11, 0.2);
      color: var(--warning);
      border: 1px solid var(--warning);
    }

    .badge-planned {
      background: rgba(99, 102, 241, 0.2);
      color: var(--planned);
      border: 1px solid var(--planned);
    }

    .status-dot {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      display: inline-block;
    }

    .dot-success {
      background: var(--success);
      box-shadow: 0 0 8px var(--success);
    }

    .dot-warning {
      background: var(--warning);
      box-shadow: 0 0 8px var(--warning);
    }

    .dot-planned {
      background: var(--planned);
      box-shadow: 0 0 8px var(--planned);
    }

    .services-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 1.5rem;
    }

    .service-card {
      background: var(--bg-secondary);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 1.5rem;
      transition: all 0.3s ease;
      text-decoration: none;
      color: inherit;
      display: block;
      position: relative;
    }

    .service-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 12px 24px rgba(59, 130, 246, 0.2);
      border-color: var(--accent);
    }

    .service-card.planned {
      opacity: 0.7;
      cursor: default;
      pointer-events: none;
    }

    .service-card.planned:hover {
      transform: none;
      box-shadow: none;
      border-color: var(--border);
    }

    .service-card.non-working {
      border-color: var(--warning);
      background: linear-gradient(135deg, var(--bg-secondary) 0%, rgba(245, 158, 11, 0.05) 100%);
    }

    .service-icon {
      width: 48px;
      height: 48px;
      background: var(--bg-card);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      margin-bottom: 1rem;
    }

    .service-name {
      font-size: 1.25rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
      color: var(--text-primary);
    }

    .service-description {
      color: var(--text-secondary);
      font-size: 0.95rem;
      line-height: 1.5;
      margin-bottom: 0.75rem;
    }

    .service-url {
      color: var(--accent);
      font-size: 0.875rem;
      font-family: monospace;
    }

    .service-card.planned .service-url {
      color: var(--text-secondary);
      opacity: 0.6;
    }

    .service-note {
      margin-top: 0.5rem;
      padding: 0.5rem;
      background: rgba(245, 158, 11, 0.1);
      border-radius: 6px;
      color: var(--warning);
      font-size: 0.8rem;
      line-height: 1.4;
    }

    .info-note {
      background: rgba(99, 102, 241, 0.1);
      color: var(--planned);
    }

    .planned-badge {
      position: absolute;
      top: 1rem;
      right: 1rem;
      background: var(--planned);
      color: white;
      padding: 0.25rem 0.5rem;
      border-radius: 4px;
      font-size: 0.7rem;
      font-weight: 600;
      text-transform: uppercase;
    }

    footer {
      text-align: center;
      margin-top: 3rem;
      padding-top: 2rem;
      border-top: 2px solid var(--border);
      color: var(--text-secondary);
      font-size: 0.875rem;
    }

    .footer-stats {
      display: flex;
      justify-content: center;
      gap: 2rem;
      margin-bottom: 1rem;
      flex-wrap: wrap;
    }

    .stat {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 0.25rem;
    }

    .stat-value {
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--accent);
    }

    .stat-label {
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .features-list {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
    }

    .feature-card {
      background: linear-gradient(135deg, var(--bg-secondary) 0%, rgba(99, 102, 241, 0.05) 100%);
      border: 1px solid var(--planned);
      border-radius: 12px;
      padding: 1.5rem;
    }

    .feature-header {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1rem;
    }

    .feature-icon {
      width: 48px;
      height: 48px;
      background: var(--bg-card);
      border-radius: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 1.5rem;
      flex-shrink: 0;
    }

    .feature-title-group {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      flex: 1;
    }

    .feature-name {
      font-size: 1.25rem;
      font-weight: 600;
      color: var(--text-primary);
      margin: 0;
    }

    .feature-priority {
      padding: 0.25rem 0.5rem;
      background: rgba(99, 102, 241, 0.2);
      color: var(--planned);
      border: 1px solid var(--planned);
      border-radius: 4px;
      font-size: 0.75rem;
      font-weight: 600;
    }

    .feature-description {
      color: var(--text-secondary);
      line-height: 1.6;
      margin-bottom: 1rem;
    }

    .feature-benefits {
      margin: 0;
      padding-left: 1.5rem;
      color: var(--text-secondary);
      font-size: 0.9rem;
    }

    .feature-benefits li {
      margin-bottom: 0.5rem;
      line-height: 1.5;
    }

    .feature-benefits li::marker {
      color: var(--planned);
    }

    @media (max-width: 768px) {
      h1 {
        font-size: 2rem;
      }

      .services-grid {
        grid-template-columns: 1fr;
      }

      .footer-stats {
        flex-direction: column;
        gap: 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1>Viljo Infrastructure Services</h1>
      <p class="subtitle">Direct access to all deployed and planned services</p>
    </header>

    {% set working_services = infrastructure_services | selectattr('status', 'equalto', 'deployed_working') | list %}
    {% set nonworking_services = infrastructure_services | selectattr('status', 'equalto', 'deployed_nonworking') | list %}
    {% set planned_services = infrastructure_services | selectattr('status', 'equalto', 'planned') | list %}

    <!-- Working Services -->
    {% if working_services | length > 0 %}
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">Available Services</h2>
        <span class="section-badge badge-success">
          <span class="status-dot dot-success"></span>
          {{ working_services | length }} Online
        </span>
      </div>
      <div class="services-grid">
        {% for service in working_services %}
        <a href="https://{{ service.subdomain }}.{{ demo_site_external_domain }}" class="service-card">
          <div class="service-icon">{{ service.icon }}</div>
          <div class="service-name">{{ service.name }}</div>
          <div class="service-description">{{ service.description }}</div>
          <div class="service-url">{{ service.subdomain }}.{{ demo_site_external_domain }}</div>
        </a>
        {% endfor %}
      </div>
    </section>
    {% endif %}

    <!-- Non-Working Services -->
    {% if nonworking_services | length > 0 %}
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">Services Under Maintenance</h2>
        <span class="section-badge badge-warning">
          <span class="status-dot dot-warning"></span>
          {{ nonworking_services | length }} Issues
        </span>
      </div>
      <div class="services-grid">
        {% for service in nonworking_services %}
        <div class="service-card non-working">
          <div class="service-icon">{{ service.icon }}</div>
          <div class="service-name">{{ service.name }}</div>
          <div class="service-description">{{ service.description }}</div>
          <div class="service-url">{{ service.subdomain }}.{{ demo_site_external_domain }}</div>
          {% if service.issue is defined and service.issue %}
          <div class="service-note">🔧 {{ service.issue }}</div>
          {% endif %}
        </div>
        {% endfor %}
      </div>
    </section>
    {% endif %}

    <!-- Planned Services -->
    {% if planned_services | length > 0 %}
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">Planned Services</h2>
        <span class="section-badge badge-planned">
          <span class="status-dot dot-planned"></span>
          {{ planned_services | length }} In Roadmap
        </span>
      </div>
      <div class="services-grid">
        {% for service in planned_services %}
        <div class="service-card planned">
          <span class="planned-badge">Coming Soon</span>
          <div class="service-icon">{{ service.icon }}</div>
          <div class="service-name">{{ service.name }}</div>
          <div class="service-description">{{ service.description }}</div>
          <div class="service-url">{{ service.subdomain }}.{{ demo_site_external_domain }}</div>
          {% if service.note is defined and service.note %}
          <div class="service-note info-note">ℹ️ {{ service.note }}</div>
          {% endif %}
        </div>
        {% endfor %}
      </div>
    </section>
    {% endif %}

    <!-- Planned Features -->
    {% if planned_features is defined and planned_features | length > 0 %}
    <section class="section">
      <div class="section-header">
        <h2 class="section-title">Planned Infrastructure Features</h2>
        <span class="section-badge badge-planned">
          <span class="status-dot dot-planned"></span>
          {{ planned_features | length }} In Development
        </span>
      </div>
      <div class="features-list">
        {% for feature in planned_features %}
        <div class="feature-card">
          <div class="feature-header">
            <span class="feature-icon">{{ feature.icon }}</span>
            <div class="feature-title-group">
              <h3 class="feature-name">{{ feature.name }}</h3>
              <span class="feature-priority">{{ feature.priority }}</span>
            </div>
          </div>
          <p class="feature-description">{{ feature.description }}</p>
          {% if feature.benefits is defined and feature.benefits | length > 0 %}
          <ul class="feature-benefits">
            {% for benefit in feature.benefits %}
            <li>{{ benefit }}</li>
            {% endfor %}
          </ul>
          {% endif %}
        </div>
        {% endfor %}
      </div>
    </section>
    {% endif %}

    <footer>
      <div class="footer-stats">
        <div class="stat">
          <div class="stat-value">{{ working_services | length }}</div>
          <div class="stat-label">Services Online</div>
        </div>
        <div class="stat">
          <div class="stat-value">{{ nonworking_services | length }}</div>
          <div class="stat-label">Maintenance</div>
        </div>
        <div class="stat">
          <div class="stat-value">{{ planned_services | length }}</div>
          <div class="stat-label">Services Planned</div>
        </div>
        {% if planned_features is defined %}
        <div class="stat">
          <div class="stat-value">{{ planned_features | length }}</div>
          <div class="stat-label">Features Planned</div>
        </div>
        {% endif %}
        <div class="stat">
          <div class="stat-value">{{ (working_services | length + nonworking_services | length + planned_services | length) }}</div>
          <div class="stat-label">Total Services</div>
        </div>
      </div>
      <p>Proxmox Infrastructure - Managed with Ansible</p>
      <p>All services are secured with TLS certificates via Traefik</p>
    </footer>
  </div>
</body>
</html>

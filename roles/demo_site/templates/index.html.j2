<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{{ demo_site_title }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    :root { color-scheme: light dark; }
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      margin: 0;
      min-height: 100vh;
      display: grid;
      place-items: center;
      background: #0f172a;
      color: #e2e8f0;
    }
    main {
      padding: 3rem 4rem;
      border-radius: 1.5rem;
      background: #020617;
      box-shadow: 0 20px 40px rgba(15, 23, 42, 0.45);
      text-align: center;
      max-width: 640px;
    }
    h1 {
      margin-top: 0;
      font-size: clamp(2rem, 4vw, 3rem);
      color: {{ demo_site_primary_color }};
    }
    p {
      font-size: 1.125rem;
      line-height: 1.6;
    }
    code {
      background: rgba(148, 163, 184, 0.12);
      border-radius: 0.5rem;
      padding: 0.25rem 0.5rem;
      font-size: 0.95rem;
      display: inline-block;
    }
  </style>
</head>
<body>
  <main>
    <h1>{{ demo_site_title }}</h1>
    <p>{{ demo_site_message }}</p>
    <p>Served via <code>https://{{ demo_site_hostname }}.{{ demo_site_domain }}</code></p>
    <p>Public URL: <code>https://{{ demo_site_hostname }}.{{ demo_site_external_domain }}</code></p>
    <p><a href="/hello.html">Visit the Hello World test page</a></p>
  </main>
</body>
</html>
